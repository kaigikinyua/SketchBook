#include<iostream>
#include<stdlib.h>
#include<stdio.h>
using namespace std;
//load a files data and do the measuring
int main(){
  FILE *fptr;
  fptr=fopen("Vertices.txt","r");
  while (getc(fptr)!=EOF){
    if (getc(fptr)=="'"){
    }
  }
}
